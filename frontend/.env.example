# URL of the Flask backend API.
# For local development the default (http://127.0.0.1:5000) is used when this is unset.
# Set this to your deployed backend URL for production, e.g.:
# REACT_APP_API_URL=https://multimodal-stress-detection-api.onrender.com
REACT_APP_API_URL=http://127.0.0.1:5000
